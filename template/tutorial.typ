#import "@preview/mitex:0.2.4": *

*ŞABLONU KULLANMAK İÇİN ÖNCE PROJE TÜRÜ SEÇMELİSİNİZ*

Main.tex dosyasını açınız\, 38. satırdaki değeri aşağıdaki kurallara göre değiştiriniz\
Bu dosyada aşağıdaki kısımdaki değeri değiştirmeniz yeterlidir\
\
Halihazırda *"projecttype değişkenindeki 0"* olan ilgili değeri\; \
AR-GE PROJELERİ için 1\,\
YAZILIM PROJELERİ için 2 \
LATEX TUTORIAL için 3 olarak değiştiriniz.

#figure(caption: [PROJE TÜRÜ SEÇİMİ])[
  ```tex
  Some stuff idk
  ```
]<fig:enter-label>

*TO USE THE TEMPLATE\, YOU MUST FIRST SELECT THE PROJECT TYPE*

Open the Main.tex file and change the value on line 38 according to the following rules.\ It is sufficient to change the value in the section below.\

Currently\, change the relevant value in *"the projecttype variable which is 0"* to: \ 1 for Ramp D PROJECTS\, \ 2 for SOFTWARE \(SW\) PROJECTS\, \ 3 for LATEX TUTORIAL.
#figure(caption: [PROJECT TYPE SELECTION])[
  ```tex
  Some other stuff idk
  ```
];<fig:enter-label>
